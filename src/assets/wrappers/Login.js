import styled from "styled-components";

const Wrapper = styled.section`
  min-height: 100vh;
  display: grid;
  align-items: center;
  max-width: 30rem;
  min-width: 20rem;
  margin: 0 auto;
`;

export default Wrapper;